---
interface Props {
  data: Record<string, unknown>
}

// Escapa `<` para que nenhum texto do JSON feche a tag script que o envolve.
const json = JSON.stringify(Astro.props.data).replace(/</g, "\\u003c")
---

<script type="application/ld+json" is:inline set:html={json}></script>
